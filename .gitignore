node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Firebase service account should stay local only
firebase-service-account.json

# Ignore accidental nested git metadata backup and nested package created by wrong cwd commands
ecms-be/.git-nested-backup-*
ecms-be/ecms-be/
