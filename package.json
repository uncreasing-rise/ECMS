{
  "scripts": {
    "start": "npm --prefix ecms-be run start",
    "start:dev": "npm --prefix ecms-be run start:dev",
    "seed:question-bank": "npm --prefix ecms-be run seed:question-bank"
  },
  "dependencies": {
    "@nestjs/config": "^4.0.3",
    "@nestjs/passport": "^11.0.5",
    "@nestjs/swagger": "^11.2.6",
    "@prisma/client": "^7.6.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/passport-jwt": "^4.0.1",
    "prisma": "^7.6.0"
  }
}
